{
    "31337": {
        "utilitytoken": {
            "address": "${process.env.REACT_APP_UTILITY_TOKEN_ADDRESS}"
        },
        "nft": {
            "address": "${process.env.REACT_APP_NFT_ADDRESS}"
        }
    }
}